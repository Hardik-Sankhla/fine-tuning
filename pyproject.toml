[project]
name = "fine-tuning"
version = "0.1.0"
description = "Fine-tuning recipes and tools for LoRA adapters and reasoning models"
authors = ["Hardik Sankhla <datascientist.hardiksankhla@gmail.com>"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
